package calculator;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.ArrayList;
import java.util.List;
import org.junit.jupiter.api.Test;


class TestType {

  @Test // Test 1
  void testSize() { // Check if no. of constants in Type is equal to no. of elements in the list
    List<Type> list = new ArrayList<Type>();
    for (Type t : Type.values()) {
      list.add(t);
    }
    assertEquals(list.size(), 4, "Test to see if all the type values added to the list is 4.");
  }
}
