package calculator;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.ArrayList;
import java.util.List;
import org.junit.jupiter.api.Test;


class TestSymbol {

  @Test // Test 1
  void testSize() { // Check if no. of constants in Symbol is equal to no. of elements in the list
    List<Symbol> list = new ArrayList<Symbol>();
    for (Symbol s : Symbol.values()) {
      list.add(s);
    }
    assertEquals(list.size(), 7, "Test to see if all the symbol values added to the list is 7.");
  }

}
